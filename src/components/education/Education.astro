---
import Timelime from '../timeline/Timelime.astro';
import { getLangFromUrl } from '../../i18n/utils';

interface Education {
  title: string;
  subtitle: string;
  description: string;
}

const lang = getLangFromUrl(Astro.url);

const title =
  lang === 'en'
    ? 'Bachelor of Computer Engineering'
    : 'Bachiller en Ingeniería en Sistemas';
---

<ol class='relative border-s border-blue-600 dark:border-gray-700'>
  <Timelime
    title={title}
    subtitle='2018-2021'
    description='Universidad Nacional de Costa Rica'
  />
</ol>
